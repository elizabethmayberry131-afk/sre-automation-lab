apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-platform.fullname" . }}-test-connection"
  labels:
    {{- include "web-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
