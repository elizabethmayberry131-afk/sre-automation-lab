apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-platform.fullname" . }}
  labels:
    {{- include "web-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-platform.selectorLabels" . | nindent 4 }}
